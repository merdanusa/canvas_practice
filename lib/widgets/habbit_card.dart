import 'package:flutter/material.dart';

class HabbitCard extends StatelessWidget {
  const HabbitCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}
